import 'package:flutter/material.dart';

class AddSectionView extends StatelessWidget {
  const AddSectionView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
