import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors
  static const Color lightBackground = Color(0xFFFDFDFD);
  static const Color lightText = Color(0xFF111111);

  // Dark theme colors
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkText = Color(0xFFECECEC);

  // Common colors
  static const Color primary = Color(0xFF00B894); // أخضر زمردي هادئ
  static const Color secondary = Color(0xFF0984E3); // أزرق فاتح
  static const Color background = Color(0xFFF1F2F6); // رمادي فاتح عصري
  static const Color text = Color(0xFF2D3436); // رمادي غامق للنصوص
  static const Color accent = Color(0xFFFFC312); // أصفر مشرق للنقاط المهمة
  static const Color error = Color(0xFFEB4D4B); // أحمر للأخطاء
}
